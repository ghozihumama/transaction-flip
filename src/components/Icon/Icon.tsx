import {createIconSetFromIcoMoon} from 'react-native-vector-icons';
import flipIconConfig from './selection.json';
/** === ICON CREATED BY ICOMOON === */
export const FlipIcon = createIconSetFromIcoMoon(flipIconConfig);
